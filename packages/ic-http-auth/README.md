# ic-http-auth

This canister side library is used to verify HTTP Message Signatures.

## Usage

See the [`todo_routes.rs`](../../examples/todo-app/src/backend/src/todo/todo_routes.rs) file in the todo app example for a usage example.

## Benchmarks

The [`canbench_results.yml`](./canbench_results.yml) file contains the results of the benchmarks.
